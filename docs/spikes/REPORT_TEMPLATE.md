# Spike 判定报告：<Sx 标题>

> 模板依据 issue #4「判定报告按统一模板（环境快照/步骤/原始输出/判定/回退触发条件）
> 入 `docs/adr/` 并引用脚本路径」。定稿后本文件移入 `docs/adr/` 并编号。

## 元信息

| 项 | 值 |
| --- | --- |
| Spike 编号 | Sx |
| 对应 issue | #4 |
| 脚本路径 | `scripts/spikes/<script>` |
| 执行日期 | YYYY-MM-DD |
| 判定 | Go / No-Go |

## 环境快照

- dsh 版本：（精确到 rc 位号，`dsh --version` 输出）
- Node 版本：
- 操作系统：
- 隔离方式：`DSH_HOME=$(mktemp -d)`
- 涉及 profile：

## 步骤

1. …

## 原始输出

```
<关键原始输出摘录；完整输出见脚本执行时落盘的日志>
```

## 判定

对照 issue #4 预注册判据逐条给出结论：

- 判据原文 → 实测 → 符合/不符合

## 回退触发条件

- （No-Go 时）触发预注册回退方向：<原文引用>；已产出/暂缓 ADR：<编号或说明>
- （Go 时）无回退；失效条件：<dsh minor/rc 高于记录版本时重跑>
